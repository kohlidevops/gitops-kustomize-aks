apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gitops-dev

resources:
  - ../../base
  - namespace.yaml

replicas:
  - name: gitops-nginx
    count: 1

labels:
  - pairs:
      environment: dev
